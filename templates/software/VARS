URL=git://github.com/KeckCAVES/$SW.git

VARS+=(VRUIVER)
if [[ -d "$CACHEDIR"/Vrui.git && -z "$VRUIVER" ]]; then
  VRUIVER=$(git --git-dir="$CACHEDIR"/Vrui.git/ tag | sed 's/^v\([0-9]*\.[0-9]*\).*$/\1/p;d' | sort -u | tail -n 1)
fi
